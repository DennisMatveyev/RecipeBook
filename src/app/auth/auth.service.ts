// Authentication backend is Firebase
export class AuthService {
  signupUser(email: string, password: string) {
    // here Firebase API goes
  }

  signinUser(email: string, password: string) {
    // here Firebase API goes
  }
}
